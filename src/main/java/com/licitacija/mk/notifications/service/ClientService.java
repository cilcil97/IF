//package com.licitacija.mk.notifications.service;////import com.licitacija.mk.models.exceptions.NotFoundException;//import com.licitacija.mk.notifications.domain.Client;//import com.licitacija.mk.notifications.repository.ClientRepository;//import org.springframework.security.core.userdetails.UserDetails;//import org.springframework.security.core.userdetails.UserDetailsService;//import org.springframework.security.core.userdetails.UsernameNotFoundException;//import org.springframework.stereotype.Service;////@Service("userDetailsService")//public class ClientService implements UserDetailsService {////    private final ClientRepository repository;////    public ClientService(ClientRepository repository) {//        this.repository = repository;//    }////    @Override//        public UserDetails loadUserByUsername(String username) throws NotFoundException{//        Client client = repository.findByUsername(username);//        if (client == null) {//            throw new UsernameNotFoundException(String.format("User %s does not exist!", username));//        }//        return (UserDetails) new Client(client);//    }//    public Client addUser(Client client) {//        return repository.save(client);//    }////}