package com.licitacija.mk.controllers.rest;import com.licitacija.mk.models.User;import com.licitacija.mk.service.UserService;import org.springframework.http.HttpStatus;import org.springframework.web.bind.annotation.*;import javax.servlet.http.HttpServletResponse;import java.util.Map;@RestController@RequestMapping(value = "/user")@CrossOrigin("*")public class UserController {    private final UserService service;    public UserController(UserService service) {        this.service = service;    }    @GetMapping("/{id}")    public User getUserById(@PathVariable("id") long id) {        return service.findUserById(id);    }    @PostMapping    @ResponseStatus(HttpStatus.CREATED)    public void addUser(@RequestBody Map<String, String> payload, HttpServletResponse response) {        service.addNewUser(payload.get("userName"), payload.get("email"), payload.get("phone"), payload.get("fullName"), payload.get("cityName"));        //response.setHeader("Location");        //TODO RETURN TO LOGIN PAGE    }    @PatchMapping("/{id}")    @ResponseStatus(HttpStatus.OK)    public void updateUser(@PathVariable("id") long id, @RequestBody Map<String, String> payload) {        service.updateUser(id, payload.get("userName"), payload.get("email"), payload.get("phone"), payload.get("fullName"), payload.get("cityName"));    }    @DeleteMapping("/{id}")    public void deleteUser(@PathVariable("id") long id) {        service.deleteUser(id);    }}